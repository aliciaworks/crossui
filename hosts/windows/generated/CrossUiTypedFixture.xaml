<!-- Generated by CrossUI. Do not edit. -->
<UserControl
    x:Class="CrossUi.Generated.CrossUiTypedFixture"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer
        HorizontalScrollBarVisibility="Disabled"
        HorizontalContentAlignment="Stretch"
        VerticalScrollBarVisibility="Auto">
        <Grid x:Name="CrossUiTypedFixtureContentRoot" Padding="32,24,32,32">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="CrossUiTypedFixtureContentRootLayoutStates">
                    <VisualState x:Name="CrossUiTypedFixtureContentRootNarrow">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="0" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <Setter Target="CrossUiTypedFixtureContentRoot.Padding" Value="16,16,16,24" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="CrossUiTypedFixtureContentRootExpanded">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="640" />
                        </VisualState.StateTriggers>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <Grid MaxWidth="840" HorizontalAlignment="Center">
                <!-- crossui-node:fixture -->
                <StackPanel Spacing="16">
                    <!-- crossui-node:fixture-content -->
                    <StackPanel Orientation="Vertical" Spacing="16">
                        <!-- crossui-node:fixture-email -->
                        <TextBox Text="{x:Bind State.Email, Mode=TwoWay}" PlaceholderText="Email address" Tag="email_changed" />
                        <!-- crossui-node:fixture-status -->
                        <TextBlock Text="{x:Bind State.Status, Mode=OneWay}" Style="{ThemeResource BodyTextBlockStyle}" TextWrapping="Wrap" />
                        <!-- crossui-node:fixture-loading -->
                        <ProgressRing IsActive="True" Visibility="{x:Bind BooleanToVisibility(State.IsSubmitting), Mode=OneWay}" />
                        <!-- crossui-node:fixture-submit -->
                        <Button Content="{x:Bind LocalizedFixtureSubmitLabel, Mode=OneTime}" Tag="submit" Click="OnAction" Style="{StaticResource AccentButtonStyle}" IsEnabled="{x:Bind State.CanSubmit, Mode=OneWay}" />
                        <!-- crossui-node:fixture-dark-mode -->
                        <ToggleSwitch Header="{x:Bind LocalizedFixtureDarkModeLabel, Mode=OneTime}" IsOn="{x:Bind State.DarkMode, Mode=TwoWay}" Tag="dark_mode_changed" />
                        <!-- crossui-node:fixture-appointment -->
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <CalendarDatePicker Date="{x:Bind State.AppointmentDate, Mode=TwoWay}" />
                            <TimePicker SelectedTime="{x:Bind State.AppointmentTime, Mode=TwoWay}" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
